---
title: "R Markdown and Leaflet"
author: "Felipe Alves"
date: "7/18/2019"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r pressure, echo=FALSE}
cities <- read.csv("https://github.com/felipe88alves/R-Markdown-and-Leaflet/blob/master/cities.csv")
head(cities)
```

```{r prepare_data, echo=FALSE}
total_time <- seq.Date(from = as.Date("1987-04-23"), to = Sys.Date(),by ="week")
df2 <- df %>%
  mutate(
    DatetoTake = case_when(
      weekday %in% c('Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday') ~ as.Date(Date),
      weekday == 'Saturday'~as.Date(Date) + 2,
      weekday == 'Sunday'~as.Date(Date) + 1
      )
  )
  
}

```

